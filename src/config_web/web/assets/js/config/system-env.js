import {appState, byId, registerRuntime, runtimeFunction} from './state.js';
const setBanner=runtimeFunction('setBanner');const setDetails=runtimeFunction('setDetails');const request=runtimeFunction('request');const refreshAgentStatus=runtimeFunction('refreshAgentStatus');const t=runtimeFunction('t');
let systemEnvSnapshot='';
function setSystemEnvLocked(locked){const editor=byId('system_env_content');const save=byId('save-system_env');const comment=byId('comment-system_env');if(editor)editor.disabled=locked;if(save)save.disabled=locked;if(comment)comment.disabled=locked;}
    function enterSystemEnvEdit(){systemEnvSnapshot=byId('system_env_content').value;setSystemEnvLocked(false);byId('section-system_env').classList.add('editing');setBanner(t('system_env.editing'),false);setDetails('');}
    function cancelSystemEnvEdit(){byId('system_env_content').value=systemEnvSnapshot;setSystemEnvLocked(true);byId('section-system_env').classList.remove('editing');setBanner(t('system_env.cancelled'),false);setDetails('');}
    function selectedSystemEnvLineRange(el){const value=el.value;const rawStart=el.selectionStart||0;const rawEnd=el.selectionEnd||rawStart;const start=rawStart<=0?0:value.lastIndexOf('\n',rawStart-1)+1;let endCursor=rawEnd;if(rawEnd>rawStart&&value.charAt(rawEnd-1)==='\n'){endCursor=rawEnd-1;}const endBreak=value.indexOf('\n',endCursor);const end=endBreak<0?value.length:endBreak;return {start:start,end:end};}
    function isBlankSystemEnvLine(line){return /^\s*$/.test(line);}
    function isCommentedSystemEnvLine(line){return /^\s*#/.test(line);}
    function uncommentSystemEnvLine(line){return line.replace(/^(\s*)# ?/,'$1');}
    function commentSystemEnvLine(line){if(isBlankSystemEnvLine(line)||/^\s*#/.test(line))return line;return line.replace(/^(\s*)/,'$1# ');}
    function replaceSystemEnvSelection(el,range,text){const nextEnd=range.start+text.length;el.focus();el.setSelectionRange(range.start,range.end);let replaced=false;if(document.execCommand){try{replaced=document.execCommand('insertText',false,text);}catch(err){replaced=false;}}if(!replaced){if(el.setRangeText){el.setRangeText(text,range.start,range.end,'select');}else{el.value=el.value.slice(0,range.start)+text+el.value.slice(range.end);el.setSelectionRange(range.start,nextEnd);}el.dispatchEvent(new Event('input',{bubbles:true}));}el.setSelectionRange(range.start,nextEnd);}
    function toggleSystemEnvComment(){const el=byId('system_env_content');if(!el||el.disabled)return;const range=selectedSystemEnvLineRange(el);const block=el.value.slice(range.start,range.end);const lines=block.split('\n');const uncommentedLines=lines.filter(function(line){return !isBlankSystemEnvLine(line)&&!isCommentedSystemEnvLine(line);});const commentedLines=lines.filter(isCommentedSystemEnvLine);if(!uncommentedLines.length&&!commentedLines.length)return;const shouldUncomment=uncommentedLines.length===0&&commentedLines.length>0;const toggled=lines.map(function(line){return shouldUncomment&&isCommentedSystemEnvLine(line)?uncommentSystemEnvLine(line):commentSystemEnvLine(line);}).join('\n');replaceSystemEnvSelection(el,range,toggled);}
    function handleSystemEnvEditorKeydown(event){if((event.ctrlKey||event.metaKey)&&event.key==='/'){event.preventDefault();toggleSystemEnvComment();}}
    async function saveSystemEnv(){byId('save-system_env').disabled=true;try{const payload=await request('/api/system/env',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({system_env:byId('system_env_content').value})});appState.systemEnv=payload.system_env||byId('system_env_content').value;byId('system_env_content').value=appState.systemEnv;setSystemEnvLocked(true);byId('section-system_env').classList.remove('editing');setBanner(t('system_env.saved'),false);setDetails('');setTimeout(function(){refreshAgentStatus(false);},1200);}catch(err){setBanner(t('system_env.save_failed'),true);setDetails(err.message);setSystemEnvLocked(false);}}

export { setSystemEnvLocked, enterSystemEnvEdit, cancelSystemEnvEdit, toggleSystemEnvComment, handleSystemEnvEditorKeydown, saveSystemEnv };
registerRuntime({ setSystemEnvLocked, enterSystemEnvEdit, cancelSystemEnvEdit, toggleSystemEnvComment, handleSystemEnvEditorKeydown, saveSystemEnv });
