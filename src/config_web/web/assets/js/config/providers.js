import {appState, byId, modelProvidersByName, registerRuntime, runtimeFunction, runtimeObject} from './state.js';

const selectFieldOptions=runtimeObject('getSelectFieldOptions');
const recordSectionFields=runtimeObject('getRecordSectionFields');
const optionValue=runtimeFunction('optionValue');
const hydrateSelectField=runtimeFunction('hydrateSelectField');
const applyFieldVisibility=runtimeFunction('applyFieldVisibility');
const ensureSelectOption=runtimeFunction('ensureSelectOption');
const request=runtimeFunction('request');
const setBanner=runtimeFunction('setBanner');
const setDetails=runtimeFunction('setDetails');
const t=runtimeFunction('t');
const getActiveLocale=runtimeFunction('getActiveLocale');
const isSectionEditing=runtimeFunction('isSectionEditing');
const resolveModelProviderType=runtimeFunction('resolveModelProviderType');

let lastModelProviderValue='';
const lastModelByProvider={};
const lastVoiceProviderValue={};

function currentModelValue(){const el=byId('model_model');return el?String(el.value||''):'';}
function rememberModelChoice(providerRef,modelValue){const provider=String(providerRef||'');if(!provider)return;const model=String(modelValue==null?currentModelValue():modelValue);if(model.trim())lastModelByProvider[provider]=model;else delete lastModelByProvider[provider];}
function setCurrentModelValue(modelValue){const model=String(modelValue||'');const input=byId('model_model');if(input)input.value=model;ModelSelector.selectedModel=model;rememberModelChoice(ModelSelector.currentProvider||lastModelProviderValue,model);syncModelSelectorSummary();}
function rememberModelProvider(){const el=byId('model_provider');if(!el)return;const previous=lastModelProviderValue;const currentModel=currentModelValue();lastModelProviderValue=el.value;if(previous&&previous!==el.value)rememberModelChoice(previous);else if(el.value&&currentModel)rememberModelChoice(el.value,currentModel);}
function renameRememberedModelProvider(oldName,newName){if(!oldName||!newName||oldName===newName)return;if(Object.prototype.hasOwnProperty.call(lastModelByProvider,oldName)){lastModelByProvider[newName]=lastModelByProvider[oldName];delete lastModelByProvider[oldName];}}
function restoreModelProviderValue(){const el=byId('model_provider');if(!el)return;ensureSelectOption(el,lastModelProviderValue);el.value=lastModelProviderValue;}
function selectModelProvider(name){const el=byId('model_provider');if(!el)return;ensureSelectOption(el,name);el.value=name;el.dispatchEvent(new Event('change',{bubbles:true}));}
function refreshModelSelectorForCurrentProvider(){const el=byId('model_provider');const value=el?String(el.value||''):'';if(value)ModelSelector.onProviderChange(value);}

function providerAPIKeyPlaceholder(type,credentialConfigured){if(credentialConfigured)return 'provider.credential_saved_placeholder';return String(type||'').trim().toLowerCase()==='anthropic'?'provider.anthropic_api_key_placeholder':'provider.api_key_placeholder';}
function providerAPIKeyHelp(type){return String(type||'').trim().toLowerCase()==='anthropic'?'provider.anthropic_api_key_help':'provider.api_key_help';}
function syncProviderCredentialHelp(section,credentialConfigured){const typeEl=byId(section+'_type');const input=byId(section+'_api_key');const field=input&&input.closest?input.closest('.field'):null;const help=field&&field.querySelector?field.querySelector('small'):null;if(!typeEl||!input||!help)return;input.placeholder=t(providerAPIKeyPlaceholder(typeEl.value,credentialConfigured));help.textContent=t(providerAPIKeyHelp(typeEl.value));}

function addProviderRename(config,section,oldName,newName){if(!oldName||!newName||oldName===newName)return config;const patch=config||{};const renames=patch._provider_renames||{};const sectionRenames=renames[section]||{};sectionRenames[newName]=oldName;renames[section]=sectionRenames;patch._provider_renames=renames;return patch;}
let providerWriteQueue=Promise.resolve();
function queueProviderWrite(write){const result=providerWriteQueue.then(write);providerWriteQueue=result.catch(function(){});return result;}
function cloneProviderRecords(records){const copy={};Object.keys(records||{}).forEach(function(name){copy[name]=Object.assign({},records[name]||{});});return copy;}
function providerRecordsMatch(section,expected,actual){const fields=recordSectionFields[section]||{};return Object.keys(actual||{}).every(function(name){const got=actual[name];if(got==null)return true;const want=expected&&expected[name];if(!want)return false;return fields.every(function(item){const key=item[0];const secret=item[2];if(secret||!Object.prototype.hasOwnProperty.call(got,key))return true;return String(want[key]||'')===String(got[key]||'');});});}
function sanitizeProviderName(value){const clean=String(value==null?'':value).trim();return ['__proto__','constructor','prototype'].indexOf(clean.toLowerCase())===-1?clean:'';}
function providerNameSlug(value){return sanitizeProviderName(String(value==null?'':value).replace(/[^A-Za-z0-9_-]+/g,'-').replace(/^-+|-+$/g,''));}
function providerCredentialConfigured(record){return !!record&&(!!record.has_api_key||!!record.api_key);}
function assignProviderAPIKey(record,apiKeyRaw){if(apiKeyRaw==='$'){alert(t('provider.env_required'));return false;}if(apiKeyRaw)record.api_key=apiKeyRaw;return true;}
function escRecordHtml(value){return String(value==null?'':value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');}
function hostLabel(url){let host=String(url||'').trim();const scheme=host.indexOf(':'+'/'+'/');if(scheme!==-1)host=host.slice(scheme+3);host=host.split('/')[0].split('?')[0];const at=host.lastIndexOf('@');if(at!==-1)host=host.slice(at+1);host=host.replace(/:\d+$/,'').replace(/^\[|\]$/g,'');if(!host)return '';if(/^[0-9.]+$/.test(host)||host.indexOf(':')!==-1)return providerNameSlug(host);const labels=host.split('.').filter(function(part){return part.length>0;});if(labels.length===0)return '';if(labels.length===1)return providerNameSlug(labels[0]);let end=labels.length-1;const secondLevel={com:true,co:true,net:true,org:true,gov:true,edu:true,ac:true};if(end>1&&secondLevel[labels[end-1].toLowerCase()])end--;return providerNameSlug(labels[end-1]);}
function ensureBlankTypeOption(section){const fields=selectFieldOptions[section];if(!fields||!Array.isArray(fields.type))return;if(fields.type.length&&optionValue(fields.type[0])==='')return;fields.type=[{value:'',label:t('provider.select'),providers:[]}].concat(fields.type);}
function recordSecretConfigured(record,key){return !!record&&(!!record['has_'+key]||!!record[key]);}
function providerFieldLabel(key){if(key==='type')return t('provider.type');if(key==='api_key')return t('provider.api_key');if(key==='base_url')return t('provider.base_url_optional');return key;}
function providerRecordFieldsHtml(section,record){const fields=recordSectionFields[section]||[];const opts=selectFieldOptions[section]||{};let html='';fields.forEach(function(item){const key=item[0];const secret=item[2];const id=section+'_'+key;const raw=!secret&&record&&record[key]!=null?String(record[key]):'';const configured=secret&&recordSecretConfigured(record,key);html+='<div class="field"><label>'+escRecordHtml(providerFieldLabel(key))+'</label>';if(Array.isArray(opts[key])&&opts[key].length){html+='<select id="'+id+'"></select>';}else if(secret){html+='<input type="password" id="'+id+'" value="" placeholder="'+escRecordHtml(t(configured?'provider.credential_saved_placeholder':'provider.credential_placeholder'))+'" autocomplete="off">';}else{html+='<input type="text" id="'+id+'" value="'+escRecordHtml(raw)+'">';}if(key==='api_key')html+='<small>'+escRecordHtml(t('provider.api_key_help'))+'</small>';html+='</div>';});return html;}

function providerRecordOptions(records){return Object.keys(records||{}).sort().map(function(name){const type=String((records[name]||{}).type||'');return {value:name,label:type?name+' ('+type+')':name,providers:[]};});}
function namedProviderOptions(){return providerRecordOptions(ModelProvidersManager.records);}
function providerManagerByName(name){if(name==='ModelProvidersManager')return ModelProvidersManager;if(name==='TtsProvidersManager')return TtsProvidersManager;if(name==='SttProvidersManager')return SttProvidersManager;return null;}
function rememberVoiceProvider(spec){const el=byId(spec.refFieldId);if(el)lastVoiceProviderValue[spec.refSection]=el.value;}
function rememberProviderSelection(spec){if(spec.refSection==='model')rememberModelProvider();else rememberVoiceProvider(spec);}
function injectProviderOptions(spec){const manager=providerManagerByName(spec.global);if(spec.refSection==='model'){Object.keys(modelProvidersByName).forEach(function(key){delete modelProvidersByName[key];});Object.assign(modelProvidersByName,manager.records||{});}const fields=selectFieldOptions[spec.refSection];if(!fields||!Array.isArray(fields.provider))return;const el=byId(spec.refFieldId);const current=el?String(el.value||''):'';const options=spec.refSection==='model'?namedProviderOptions():providerRecordOptions(manager.records);const known={};options.forEach(function(option){known[optionValue(option)]=true;});if(current&&!known[current])options.push({value:current,label:current,providers:[]});options.unshift({value:'',label:'-- Select Provider --',providers:[]});fields.provider=options;hydrateSelectField(spec.refSection,'provider',current,true);rememberProviderSelection(spec);updateProviderActionState(spec.refSection);}
function injectNamedProviderOptions(){injectProviderOptions(MODEL_PROVIDER_SPEC);}

function createProviderRecordsManager(spec){return {
  spec:spec,records:{},confirmedRecords:{},writeVersion:0,nameDirty:false,pendingSelect:false,dialogCredentialConfigured:false,
  init:function(){},
  load:function(records){this.records=cloneProviderRecords(records);this.confirmedRecords=cloneProviderRecords(records);},
  render:function(){const container=byId(this.spec.listId);if(!container)return;const names=Object.keys(this.records).sort();if(names.length===0){container.innerHTML='<div class="providers-empty"><p>'+escRecordHtml(t('provider.none'))+'</p><p>'+escRecordHtml(t('provider.none_help'))+'</p></div>';return;}const self=this;container.innerHTML=names.map(function(name){return self.cardHtml(name,self.records[name]||{});}).join('');},
  cardHtml:function(name,record){const g=this.spec.global;let rows='<div class="provider-info-row"><span class="provider-info-label">'+escRecordHtml(t('provider.type_short'))+':</span><span>'+escRecordHtml(record.type||'')+'</span></div>';if(providerCredentialConfigured(record))rows+='<div class="provider-info-row"><span class="provider-info-label">'+escRecordHtml(t('provider.api_key'))+':</span><span>'+escRecordHtml(t('provider.credential_configured'))+'</span></div>';const detail=this.spec.detailKeys||[];detail.forEach(function(key){if(record[key])rows+='<div class="provider-info-row"><span class="provider-info-label">'+escRecordHtml(key)+':</span><span>'+escRecordHtml(record[key])+'</span></div>';});return '<div class="provider-card"><div class="provider-card-head"><div class="provider-name">'+escRecordHtml(name)+'</div><div class="provider-actions"><button class="button ghost small" data-action="provider-record-edit" data-provider-manager="'+g+'" data-provider-name="'+escRecordHtml(name)+'">'+escRecordHtml(t('action.edit'))+'</button><button class="button ghost small error" data-action="provider-record-delete" data-provider-manager="'+g+'" data-provider-name="'+escRecordHtml(name)+'">'+escRecordHtml(t('action.delete'))+'</button></div></div><div class="provider-info">'+rows+'</div></div>';},
  addRecord:function(options){this.pendingSelect=!!(options&&options.selectIntoRef);this.showDialog(null);},
  editRecord:function(name){const record=this.records[name];if(record)this.showDialog(name,record);},
  deleteRecord:function(name){return deleteProviderByName(this.spec.refSection,name);},
  showDialog:function(editName,editRecord){const section=this.spec.section;const record=editRecord||{};const isEdit=!!editName;this.nameDirty=isEdit;this.dialogCredentialConfigured=providerCredentialConfigured(record);const g=this.spec.global;const nameHelp=this.spec.nameHelp||'provider.name_help';const dialog=document.createElement('div');dialog.className='modal-overlay';dialog.innerHTML='<div class="modal-dialog"><div class="modal-header"><h3>'+escRecordHtml(isEdit?t('provider.edit'):t('provider.add'))+'</h3><button class="modal-close" data-action="close-modal">&times;</button></div><div class="modal-body">'+providerRecordFieldsHtml(section,record)+'<div class="field"><label>'+escRecordHtml(t('provider.name'))+'</label><input type="text" id="'+section+'_record_name" value="'+escRecordHtml(editName||'')+'" placeholder="'+escRecordHtml(t('provider.auto'))+'" data-action="provider-record-name" data-provider-manager="'+g+'"><small>'+escRecordHtml(t(nameHelp))+'</small></div></div><div class="modal-footer"><button class="button ghost" data-action="close-modal">'+escRecordHtml(t('action.cancel'))+'</button><button class="button primary" data-action="provider-record-save" data-provider-manager="'+g+'" data-edit-name="'+escRecordHtml(editName||'')+'">'+escRecordHtml(isEdit?t('provider.save_changes'):t('provider.add'))+'</button></div></div>';document.body.appendChild(dialog);this.hydrateDialog(record);},
  hydrateDialog:function(record){const section=this.spec.section;const fields=recordSectionFields[section]||[];const opts=selectFieldOptions[section]||{};ensureBlankTypeOption(section);hydrateSelectField(section,'type',record.type==null?'':String(record.type),true);const self=this;const typeEl=byId(section+'_type');if(typeEl)typeEl.addEventListener('change',function(){self.onTypeChange();});fields.forEach(function(item){const key=item[0];const el=byId(section+'_'+key);if(key!=='type'&&Array.isArray(opts[key])&&opts[key].length)hydrateSelectField(section,key,record[key]==null?'':String(record[key]),true);if(el&&key!=='type')el.addEventListener('input',function(){self.autoFillName();});});const nameEl=byId(section+'_record_name');if(nameEl)nameEl.addEventListener('input',function(){self.onNameInput();});applyFieldVisibility(true);this.autoFillName();if(this.spec.credentialHint)this.spec.credentialHint(this);setTimeout(function(){if(typeEl)typeEl.focus();},100);},
  onTypeChange:function(){const section=this.spec.section;const opts=selectFieldOptions[section]||{};(recordSectionFields[section]||[]).forEach(function(item){const key=item[0];if(key==='type'||!Array.isArray(opts[key])||!opts[key].length)return;const el=byId(section+'_'+key);hydrateSelectField(section,key,el?el.value:'',true);});applyFieldVisibility(true);this.autoFillName();if(this.spec.credentialHint)this.spec.credentialHint(this);},
  onNameInput:function(){this.nameDirty=true;},
  sanitizeName:sanitizeProviderName,
  uniqueName:function(base,editName){const clean=providerNameSlug(base);if(!clean)return '';const self=this;const taken=function(name){return Object.prototype.hasOwnProperty.call(self.records,name)&&name!==editName;};if(!taken(clean))return clean;for(let i=2;i<1000;i++){const candidate=clean+'-'+i;if(!taken(candidate))return candidate;}return clean;},
  autoFillName:function(){if(this.nameDirty)return;const section=this.spec.section;const nameEl=byId(section+'_record_name');if(!nameEl)return;const record=this.readDialog();const base=this.spec.nameBase?this.spec.nameBase(record):String(record.type||'');nameEl.value=base?this.uniqueName(base,''):'';},
  readDialog:function(){const section=this.spec.section;const record={};(recordSectionFields[section]||[]).forEach(function(item){const key=item[0];const el=byId(section+'_'+key);if(!el)return;const field=el.closest?el.closest('.field'):null;if(field&&field.classList.contains('hidden'))return;const value=String(el.value||'').trim();if(value!=='')record[key]=value;});return record;},
  saveDialog:function(editName){const section=this.spec.section;const record=this.readDialog();if(!record.type){alert(t('provider.type_required'));return;}const nameEl=byId(section+'_record_name');const base=this.spec.nameBase?this.spec.nameBase(record):record.type;let name=this.sanitizeName(nameEl?nameEl.value:'');if(!name)name=this.uniqueName(base,editName||'');if(!name){alert(t('provider.name_required'));return;}if(name!==editName&&Object.prototype.hasOwnProperty.call(this.records,name)){alert(t('provider.exists',{name:name}));return;}const apiKeyRaw=record.api_key==null?'':String(record.api_key);if(!assignProviderAPIKey(record,apiKeyRaw))return;const renamedFrom=editName&&name!==editName?editName:'';const refEl=byId(this.spec.refFieldId);const rename=renamedFrom?{oldName:renamedFrom,newName:name,selected:!!(refEl&&refEl.value===renamedFrom)}:null;if(renamedFrom)delete this.records[renamedFrom];this.records[name]=record;const selectInto=this.pendingSelect&&!editName?name:'';this.pendingSelect=false;const result=this.save(addProviderRename(renamedFrom?this.refPatch(renamedFrom,name):null,section,renamedFrom,name),selectInto,rename);const overlay=document.querySelector('.modal-overlay');if(overlay)overlay.remove();return result;},
  refPatch:function(oldName,newName){const config=appState.config||{};const current=config[this.spec.refSection];if(!current||current.provider!==oldName)return null;const copy=Object.assign({},current);copy.provider=newName;const patch={};patch[this.spec.refSection]=copy;return patch;},
  save:function(extraConfig,selectIntoRef,rename){const self=this;const snapshot=cloneProviderRecords(this.records);const version=++this.writeVersion;return queueProviderWrite(async function(){try{const recordsPatch={};const previous=self.confirmedRecords||{};const writable=function(record){const copy={};Object.keys(record||{}).forEach(function(key){if(key.indexOf('has_')!==0)copy[key]=record[key];});return copy;};Object.keys(previous).forEach(function(name){if(!Object.prototype.hasOwnProperty.call(snapshot,name))recordsPatch[name]=null;});Object.keys(snapshot).forEach(function(name){const next=writable(snapshot[name]);const old=writable(previous[name]);Object.keys(old).forEach(function(key){if(!Object.prototype.hasOwnProperty.call(next,key))next[key]=null;});if(JSON.stringify(next)!==JSON.stringify(old))recordsPatch[name]=next;});const body={};body[self.spec.configKey]=recordsPatch;const config=Object.assign(body,extraConfig||{});const payload=await request('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({config:config})});const authoritative=payload.config&&payload.config[self.spec.configKey];if(!authoritative||!providerRecordsMatch(self.spec.section,snapshot,authoritative))throw new Error(t('provider.save_not_applied'));const saved=cloneProviderRecords(authoritative);self.confirmedRecords=saved;if(version===self.writeVersion)self.records=cloneProviderRecords(saved);if(payload.config)appState.config=payload.config;else if(appState.config)appState.config[self.spec.configKey]=cloneProviderRecords(saved);if(rename){const el=byId(self.spec.refFieldId);if(rename.selected&&el){ensureSelectOption(el,rename.newName);el.value=rename.newName;}if(self.spec.onRename)self.spec.onRename(rename.oldName,rename.newName,rename.selected);}injectProviderOptions(self.spec);setDetails('');setBanner(t('provider.saved'),false);if(self.spec.afterSave)self.spec.afterSave(self,selectIntoRef);else if(selectIntoRef){const el=byId(self.spec.refFieldId);if(el){ensureSelectOption(el,selectIntoRef);el.value=selectIntoRef;}}applyFieldVisibility(true);updateProviderActionState(self.spec.refSection);return true;}catch(err){if(version===self.writeVersion){self.records=cloneProviderRecords(self.confirmedRecords);injectProviderOptions(self.spec);updateProviderActionState(self.spec.refSection);}setBanner(t('provider.save_failed'),true);setDetails(err.message);return false;}});}
};}

const MODEL_PROVIDER_SPEC={
  global:'ModelProvidersManager',section:'model_providers',configKey:'model_providers',refSection:'model',refFieldId:'model_provider',editBtnId:'editProviderBtn',deleteBtnId:'deleteProviderBtn',nameHelp:'provider.name_help_model',detailKeys:['base_url'],
  nameBase:function(record){const type=String(record.type||'');if(type==='openai'&&record.base_url){const name=hostLabel(record.base_url);if(name)return name;}return type;},
  onRename:function(oldName,newName,selected){if(selected&&currentModelValue().trim())rememberModelChoice(oldName);if(selected)rememberModelProvider();renameRememberedModelProvider(oldName,newName);},
  afterSave:function(_manager,selectIntoRef){if(selectIntoRef)selectModelProvider(selectIntoRef);else refreshModelSelectorForCurrentProvider();},
  credentialHint:function(manager){const type=String((byId('model_providers_type')||{}).value||'');const credentialConfigured=manager.dialogCredentialConfigured;const input=byId('model_providers_api_key');const field=input&&input.closest?input.closest('.field'):null;const help=field&&field.querySelector?field.querySelector('small'):null;if(!input||!help)return;input.placeholder=t(providerAPIKeyPlaceholder(type,credentialConfigured));help.textContent=t(providerAPIKeyHelp(type));}
};
const TTS_PROVIDER_SPEC={global:'TtsProvidersManager',section:'tts_providers',configKey:'tts_providers',refSection:'tts',refFieldId:'tts_provider',editBtnId:'editTtsProviderBtn',deleteBtnId:'deleteTtsProviderBtn'};
const STT_PROVIDER_SPEC={global:'SttProvidersManager',section:'stt_providers',configKey:'stt_providers',refSection:'stt',refFieldId:'stt_provider',editBtnId:'editSttProviderBtn',deleteBtnId:'deleteSttProviderBtn'};
const ModelProvidersManager=createProviderRecordsManager(MODEL_PROVIDER_SPEC);
const TtsProvidersManager=createProviderRecordsManager(TTS_PROVIDER_SPEC);
const SttProvidersManager=createProviderRecordsManager(STT_PROVIDER_SPEC);

function syncModelProvidersFromConfig(){if(appState.config&&appState.config.model_providers)ModelProvidersManager.load(appState.config.model_providers);else ModelProvidersManager.load({});injectProviderOptions(MODEL_PROVIDER_SPEC);applyFieldVisibility(true);refreshModelSelectorForCurrentProvider();}
function restoreVoiceProviderValue(spec){const el=byId(spec.refFieldId);if(!el)return;const remembered=lastVoiceProviderValue[spec.refSection]||'';ensureSelectOption(el,remembered);el.value=remembered;}
function syncVoiceProvidersFromConfig(){const config=appState.config||{};TtsProvidersManager.load(config.tts_providers||{});SttProvidersManager.load(config.stt_providers||{});injectProviderOptions(TTS_PROVIDER_SPEC);injectProviderOptions(STT_PROVIDER_SPEC);}
function bindVoiceProviderSelect(spec){document.addEventListener('change',function(e){if(!e.target||e.target.id!==spec.refFieldId)return;rememberVoiceProvider(spec);updateProviderActionState(spec.refSection);});}

const ModelSelector = {
  availableModels:[],currentProvider:null,selectedModel:null,requestId:0,
  init:function(){this.setupEventListeners();},
  onProviderChange:async function(providerRef){this.currentProvider=providerRef;if(!providerRef){this.requestId++;setCurrentModelValue('');this.clearModelSelector();return;}const providerType=this.resolveProviderType(providerRef);if(!providerType){this.requestId++;setCurrentModelValue('');this.clearModelSelector();return;}await this.loadModels(providerType);},
  resolveProviderType:function(providerRef){return resolveModelProviderType(providerRef);},
  loadModels:async function(providerType){const requestId=++this.requestId;const container=byId('modelSelectorContainer');if(!container)return;container.innerHTML='<div class="model-selector-loading">'+this.escapeHtml(t('provider.loading_models'))+'</div>';try{const locale=getActiveLocale()||'en-US';const response=await fetch(`/api/models?provider=${encodeURIComponent(providerType)}&locale=${encodeURIComponent(locale)}`);if(requestId!==this.requestId)return;if(response.status===503){this.availableModels=[];this.renderModelSelector(true);return;}if(!response.ok)throw new Error(`HTTP ${response.status}`);const data=await response.json();if(requestId!==this.requestId)return;this.availableModels=data.models||[];this.renderModelSelector();}catch(err){if(requestId!==this.requestId)return;this.availableModels=[];this.applyProviderModelChoice(true);container.innerHTML=`<div class="model-selector-error">${t('provider.load_models_failed')}: ${this.escapeHtml(err.message)}</div>`;}},
  defaultModelId:function(){const models=this.availableModels||[];const model=models.find(function(item){return item&&(item.default||item.is_default||item.recommended);})||models[0];return model&&model.id?String(model.id):'';},
  providerModelChoice:function(providerRef,agentOffline){const remembered=lastModelByProvider[String(providerRef||'')]||'';if(remembered)return remembered;return agentOffline?'':this.defaultModelId();},
  applyProviderModelChoice:function(agentOffline){setCurrentModelValue(this.providerModelChoice(this.currentProvider,agentOffline));},
  renderModelSelector:function(agentOffline){const container=byId('modelSelectorContainer');if(!container)return;this.applyProviderModelChoice(agentOffline);if(this.availableModels.length===0&&!agentOffline){container.innerHTML='<div class="model-selector-empty">'+this.escapeHtml(t('provider.no_models'))+'</div>';return;}const currentModel=(byId('model_model')||{}).value||'';let html='<div class="model-selector">';if(agentOffline)html+=`<div class="model-selector-empty">${t('provider.offline_models')}</div>`;this.availableModels.forEach((model)=>{const isSelected=model.id===currentModel;const isRecommended=model.recommended;html+=` <label class="model-option${isRecommended?' recommended':''}${isSelected?' selected':''}"> <input type="radio" name="model_select" value="${this.escapeHtml(model.id)}" ${isSelected?'checked':''} data-action="select-model" data-model-id="${this.escapeHtml(model.id)}"> <div class="model-info"> <div class="model-name"> ${this.escapeHtml(model.id)} ${isRecommended?' <span class="model-badge">⭐ '+this.escapeHtml(t('provider.recommended'))+'</span>':''} </div> <div class="model-desc">${this.escapeHtml(model.description||'')}</div> </div> </label> `;});const isCustom=currentModel&&!this.availableModels.find(function(model){return model.id===currentModel;});html+=` <label class="model-option custom${isCustom?' selected':''}"> <input type="radio" name="model_select" value="__custom__" ${isCustom?'checked':''} data-action="select-custom-model"> <div class="model-info"> <div class="model-name">${this.escapeHtml(t('provider.custom_model'))}</div> <input type="text" id="customModelInput" value="${isCustom?this.escapeHtml(currentModel):''}" placeholder="${this.escapeHtml(t('provider.model_placeholder'))}" data-action="update-custom-model"> </div> </label> `;html+='</div>';container.innerHTML=html;},
  selectModel:function(modelId){setCurrentModelValue(modelId);},
  selectCustom:function(){const customInput=byId('customModelInput');if(customInput){customInput.focus();this.updateCustomModel(customInput.value);}},
  updateCustomModel:function(value){setCurrentModelValue(value);},
  clearModelSelector:function(){const container=byId('modelSelectorContainer');if(container)container.innerHTML='';},
  setupEventListeners:function(){document.addEventListener('change',(e)=>{if(e.target.id!=='model_provider')return;rememberModelProvider();updateProviderActionState('model');this.onProviderChange(e.target.value);});},
  escapeHtml:escRecordHtml
};
function editOnlyModelSelectorMethod(method){return function(){if(!isSectionEditing('model'))return;return method.apply(this,arguments);};}
ModelSelector.selectModel=editOnlyModelSelectorMethod(ModelSelector.selectModel);
ModelSelector.selectCustom=editOnlyModelSelectorMethod(ModelSelector.selectCustom);
ModelSelector.updateCustomModel=editOnlyModelSelectorMethod(ModelSelector.updateCustomModel);

function providerSpec(kind){if(kind==='model')return MODEL_PROVIDER_SPEC;if(kind==='tts')return TTS_PROVIDER_SPEC;if(kind==='stt')return STT_PROVIDER_SPEC;return null;}
function providerContext(kind){const spec=providerSpec(kind);if(!spec)return null;const manager=providerManagerByName(spec.global);if(!manager)return null;return {manager:manager,records:manager.records,configKey:spec.configKey,refSection:spec.refSection,refFieldId:spec.refFieldId,editBtnId:spec.editBtnId,deleteBtnId:spec.deleteBtnId,add:function(){manager.addRecord();},edit:function(name){manager.editRecord(name);},refPatch:function(oldName,newName){return manager.refPatch(oldName,newName);}};}
function selectedProviderName(kind){const context=providerContext(kind);if(!context)return '';const el=byId(context.refFieldId);const name=el?String(el.value||''):'';return Object.prototype.hasOwnProperty.call(context.records||{},name)?name:'';}
function updateProviderActionState(kind){const context=providerContext(kind);if(!context)return;const enabled=!!selectedProviderName(kind);const edit=byId(context.editBtnId);const remove=byId(context.deleteBtnId);if(edit)edit.disabled=!enabled;if(remove)remove.disabled=!enabled;}
function updateAllProviderActionStates(){updateProviderActionState('model');updateProviderActionState('tts');updateProviderActionState('stt');}
function editSelectedProvider(kind){const context=providerContext(kind);if(!context)return;const name=selectedProviderName(kind);if(name)context.edit(name);}
async function deleteProviderByName(kind,name){const context=providerContext(kind);if(!context)return false;const records=context.records||{};if(!Object.prototype.hasOwnProperty.call(records,name))return false;const remainingNames=Object.keys(records).filter(function(candidate){return candidate!==name;}).sort();const storedSection=(appState.config&&appState.config[context.refSection])||{};const selected=byId(context.refFieldId);const originalSelectedValue=selected?selected.value:'';const selectedInForm=!!(selected&&selected.value===name);const storedInUse=storedSection.provider===name;if(storedInUse&&remainingNames.length===0){alert(t('provider.in_use'));return false;}const question=storedInUse?t('provider.delete_in_use_confirm'):t('provider.delete_confirm',{name:name});if(!window.confirm(question))return false;const replacement=storedInUse?remainingNames[0]:'';const uiReplacement=selectedInForm?(replacement||storedSection.provider||remainingNames[0]||''):'';const nextRecords=cloneProviderRecords(records);delete nextRecords[name];const patch=replacement?context.refPatch(name,replacement):null;context.manager.records=nextRecords;const saved=await context.manager.save(patch);if(!saved){if(selected){ensureSelectOption(selected,originalSelectedValue);selected.value=originalSelectedValue;}rememberProviderSelection(providerSpec(kind));updateAllProviderActionStates();return false;}if(selectedInForm&&selected){ensureSelectOption(selected,uiReplacement);selected.value=uiReplacement;}syncModelProvidersFromConfig();syncVoiceProvidersFromConfig();syncModelSelectorSummary();updateAllProviderActionStates();return true;}
async function deleteSelectedProvider(kind){const name=selectedProviderName(kind);if(!name)return false;return deleteProviderByName(kind,name);}
function syncModelSelectorSummary(){const input=byId('model_model');const summary=byId('modelSelectorSummary');if(!summary)return;const value=input?String(input.value||'').trim():'';summary.textContent=value||t('provider.choose_model');summary.title=value;}

export function initProviders(){bindVoiceProviderSelect(TTS_PROVIDER_SPEC);bindVoiceProviderSelect(STT_PROVIDER_SPEC);document.addEventListener('change',function(event){const target=event.target;if(!target)return;if(target.name==='model_select'){if(target.dataset.action==='select-model')ModelSelector.selectModel(target.dataset.modelId||target.value);else if(target.dataset.action==='select-custom-model')ModelSelector.selectCustom();syncModelSelectorSummary();if(target.value!=='__custom__'){const details=byId('modelSelectorDetails');if(details)details.open=false;}}else if(target.id==='customModelInput'){ModelSelector.updateCustomModel(target.value);syncModelSelectorSummary();}});document.addEventListener('input',function(event){const target=event.target;if(target&&target.id==='customModelInput'){ModelSelector.updateCustomModel(target.value);syncModelSelectorSummary();}});ModelProvidersManager.init();ModelSelector.init();TtsProvidersManager.init();SttProvidersManager.init();updateAllProviderActionStates();syncModelSelectorSummary();}
export function handleProviderAction(action,target){if(action==='close-modal'){const overlay=target.closest('.modal-overlay');if(overlay)overlay.remove();return;}const manager=providerManagerByName(target.dataset.providerManager);if(!manager)return;const name=target.dataset.providerName||'';if(action==='provider-record-edit')manager.editRecord(name);else if(action==='provider-record-delete')manager.deleteRecord(name);else if(action==='provider-record-save')manager.saveDialog(target.dataset.editName||'');}

registerRuntime({initProviders,handleProviderAction});
export {syncModelProvidersFromConfig,ModelProvidersManager,ModelSelector,TtsProvidersManager,SttProvidersManager,syncVoiceProvidersFromConfig,rememberModelProvider,restoreModelProviderValue,restoreVoiceProviderValue,updateProviderActionState,updateAllProviderActionStates,editSelectedProvider,deleteSelectedProvider,syncModelSelectorSummary,providerAPIKeyHelp,providerAPIKeyPlaceholder,syncProviderCredentialHelp};
registerRuntime({syncModelProvidersFromConfig,ModelProvidersManager,ModelSelector,TtsProvidersManager,SttProvidersManager,syncVoiceProvidersFromConfig,rememberModelProvider,restoreModelProviderValue,updateProviderActionState,updateAllProviderActionStates,editSelectedProvider,deleteSelectedProvider,syncModelSelectorSummary});
