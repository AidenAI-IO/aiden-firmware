import {appState, byId, modelProvidersByName, registerRuntime, runtimeFunction, runtimeObject} from './state.js';
const selectFieldOptions=runtimeObject('getSelectFieldOptions');const recordSectionFields=runtimeObject('getRecordSectionFields');const optionValue=runtimeFunction('optionValue');const hydrateSelectField=runtimeFunction('hydrateSelectField');const applyFieldVisibility=runtimeFunction('applyFieldVisibility');const ensureSelectOption=runtimeFunction('ensureSelectOption');const request=runtimeFunction('request');const setBanner=runtimeFunction('setBanner');const setDetails=runtimeFunction('setDetails');const t=runtimeFunction('t');const getActiveLocale=runtimeFunction('getActiveLocale');let lastModelProviderValue='';
// Sync providers from config into the manager and trigger model-selector refresh.
    // Called from loadConfig() after applyPayload so both initial load and reloadAll pick it up.
    // The [model] provider select offers only configured [model_providers.*] entries
    // and exposes Add/Edit/Delete beside the select. The config metadata enum lists built-in provider
    // types, but a bare type carries no credentials, so selecting one produced
    // a model that cannot authenticate. A provider already referenced by
    // agent.toml is kept as an option so an existing config still round-trips.
    function namedProviderOptions(){return Object.keys(modelProvidersByName).sort().map(function(name){const type=String((modelProvidersByName[name]||{}).type||'');return {value:name,label:type?name+' ('+type+')':name,providers:[]};});}
    function injectNamedProviderOptions(){Object.keys(modelProvidersByName).forEach(function(key){delete modelProvidersByName[key];});Object.assign(modelProvidersByName,ModelProvidersManager.modelProviders||{});const fields=selectFieldOptions['model'];if(!fields||!Array.isArray(fields.provider))return;const el=byId('model_provider');const current=el?String(el.value||''):'';const options=namedProviderOptions();const known={};options.forEach(function(option){known[optionValue(option)]=true;});if(current&&!known[current]){options.push({value:current,label:current,providers:[]});}options.unshift({value:'',label:'-- Select Provider --',providers:[]});fields.provider=options;hydrateSelectField('model','provider',current,true);rememberModelProvider();updateProviderActionState('model');}
    function rememberModelProvider(){const el=byId('model_provider');if(el){lastModelProviderValue=el.value;}}
    function restoreModelProviderValue(){const el=byId('model_provider');if(!el)return;ensureSelectOption(el,lastModelProviderValue);el.value=lastModelProviderValue;}
    function selectModelProvider(name){const el=byId('model_provider');if(!el)return;ensureSelectOption(el,name);el.value=name;rememberModelProvider();el.dispatchEvent(new Event('change',{bubbles:true}));}
    const PROVIDER_BASE_URL_TYPES = ['openai', 'ollama'];
    function providerBaseUrlAllowed(type) { return PROVIDER_BASE_URL_TYPES.indexOf(String(type == null ? '' : type).trim().toLowerCase()) !== -1; }
    function addProviderRename(config,section,oldName,newName){if(!oldName||!newName||oldName===newName)return config;const patch=config||{};const renames=patch._provider_renames||{};const sectionRenames=renames[section]||{};sectionRenames[newName]=oldName;renames[section]=sectionRenames;patch._provider_renames=renames;return patch;}
    function refreshModelSelectorForCurrentProvider(){const el=byId('model_provider');const value=el?String(el.value||''):'';if(!value)return;ModelSelector.onProviderChange(value);}
    function syncModelProvidersFromConfig() { if (appState.config && appState.config.model_providers) { ModelProvidersManager.loadModelProviders(appState.config.model_providers); } else { ModelProvidersManager.loadModelProviders({}); } injectNamedProviderOptions(); applyFieldVisibility(true); refreshModelSelectorForCurrentProvider(); } const ModelProvidersManager = { modelProviders: {}, pendingModelSelect: false, init: function() { this.renderModelProvidersList(); this.setupEventListeners(); }, loadModelProviders: function(providers) { this.modelProviders = providers || {}; this.renderModelProvidersList(); }, renderModelProvidersList: function() { const container = document.getElementById('providersList'); if (!container) return; container.innerHTML = ''; const providerKeys = Object.keys(this.modelProviders); if (providerKeys.length === 0) { container.innerHTML = ` <div class="providers-empty"> <p>${this.escapeHtml(t('provider.none'))}</p> <p>${this.escapeHtml(t('provider.none_help'))}</p> </div> `; return; } providerKeys.forEach(name => { const provider = this.modelProviders[name]; const card = this.createProviderCard(name, provider); container.appendChild(card); }); }, createProviderCard: function(name, provider) { const card = document.createElement('div'); card.className = 'provider-card'; card.innerHTML = ` <div class="provider-card-head"> <div class="provider-name">${this.escapeHtml(name)}</div> <div class="provider-actions"> <button class="button ghost small" data-action="model-provider-edit" data-provider-name="${this.escapeHtml(name)}">${this.escapeHtml(t('action.edit'))}</button> <button class="button ghost small error" data-action="model-provider-delete" data-provider-name="${this.escapeHtml(name)}">${this.escapeHtml(t('action.delete'))}</button> </div> </div> <div class="provider-info"> <div class="provider-info-row"> <span class="provider-info-label">${this.escapeHtml(t('provider.type'))}:</span> <span>${this.escapeHtml(provider.type || '')}</span> </div> ${(provider.token_env || provider.api_key) ? ` <div class="provider-info-row"> <span class="provider-info-label">${this.escapeHtml(t('provider.api_key'))}:</span> <span>${this.escapeHtml(provider.token_env ? '$' + provider.token_env : provider.api_key)}</span> </div> ` : ''} ${provider.base_url ? ` <div class="provider-info-row"> <span class="provider-info-label">${this.escapeHtml(t('provider.base_url'))}:</span> <span class="provider-info-url">${this.escapeHtml(provider.base_url)}</span> </div> ` : ''} </div> `; return card; }, addProvider: function(options) { this.pendingModelSelect = !!(options && options.selectIntoModel); this.showProviderDialog(null); }, editProvider: function(name) { const provider = this.modelProviders[name]; if (!provider) return; this.showProviderDialog(name, provider); }, deleteProvider: function(name) { if (!window.confirm(t('provider.delete_confirm',{name:name}))) return; delete this.modelProviders[name]; this.saveModelProviders(); }, showProviderDialog: function(editName, editProvider) { const isEdit = !!editName; const provider = editProvider || {}; const apiKeyValue = provider.token_env ? '$' + provider.token_env : (provider.api_key || ''); this.nameDirty = isEdit; const dialog = document.createElement('div'); dialog.className = 'modal-overlay'; dialog.innerHTML = ` <div class="modal-dialog"> <div class="modal-header"> <h3>${isEdit ? this.escapeHtml(t('provider.edit')) : this.escapeHtml(t('provider.add'))}</h3> <button class="modal-close" data-action="close-modal">&times;</button> </div> <div class="modal-body"> <div class="field"> <label>${this.escapeHtml(t('provider.type'))}</label> <select id="providerType" data-action="model-provider-type"> <option value="">${this.escapeHtml(t('provider.select'))}</option> <option value="openai" ${provider.type === 'openai' ? 'selected' : ''}>OpenAI</option> <option value="kimi" ${provider.type === 'kimi' ? 'selected' : ''}>Kimi</option> <option value="kimi-cn" ${provider.type === 'kimi-cn' ? 'selected' : ''}>Kimi CN</option> <option value="volcengine" ${provider.type === 'volcengine' ? 'selected' : ''}>Volcengine Ark</option> <option value="openrouter" ${provider.type === 'openrouter' ? 'selected' : ''}>OpenRouter</option> <option value="ollama" ${provider.type === 'ollama' ? 'selected' : ''}>Ollama</option> </select> </div> <div class="field"> <label>${this.escapeHtml(t('provider.api_key'))}</label> <input type="text" id="providerApiKey" value="${this.escapeHtml(apiKeyValue)}" placeholder="${this.escapeHtml(t('provider.api_key_placeholder'))}" data-action="model-provider-api-key"> <small>${this.escapeHtml(t('provider.api_key_help'))}</small> </div> <div class="field" id="providerBaseUrlField" style="display:${providerBaseUrlAllowed(provider.type) ? 'flex' : 'none'}"> <label>${this.escapeHtml(t('provider.base_url_optional'))}</label> <input type="text" id="providerBaseUrl" value="${this.escapeHtml(provider.base_url || '')}" placeholder="https://api.example.com/v1" data-action="model-provider-base-url"> </div> <div class="field"> <label>${this.escapeHtml(t('provider.name'))}</label> <input type="text" id="providerName" value="${this.escapeHtml(editName || '')}" placeholder="${this.escapeHtml(t('provider.auto'))}" data-action="model-provider-name"> <small>${this.escapeHtml(t('provider.name_help_model'))}</small> </div> </div> <div class="modal-footer"> <button class="button ghost" data-action="close-modal">${this.escapeHtml(t('action.cancel'))}</button> <button class="button primary" data-action="model-provider-save" data-edit-name="${this.escapeHtml(editName || '')}"> ${isEdit ? t('provider.save_changes') : t('provider.add')} </button> </div> </div> `; document.body.appendChild(dialog); this.autoFillProviderName(); setTimeout(() => { const firstInput = dialog.querySelector('#providerType'); if (firstInput) firstInput.focus(); }, 100); }, syncProviderBaseUrlVisibility: function() { const type = document.getElementById('providerType'); const field = document.getElementById('providerBaseUrlField'); if (!type || !field) return; field.style.display = providerBaseUrlAllowed(type.value) ? 'flex' : 'none'; }, providerBaseName: function(type, baseUrl) { const cleanType = String(type || '').trim(); const url = String(baseUrl || '').trim(); if (cleanType === 'openai' && url) { const host = this.hostLabel(url); if (host) return host; } return cleanType; }, hostLabel: function(url) { let host = String(url || '').trim(); const scheme = host.indexOf(':' + '/' + '/'); if (scheme !== -1) { host = host.slice(scheme + 3); } host = host.split('/')[0].split('?')[0]; const at = host.lastIndexOf('@'); if (at !== -1) { host = host.slice(at + 1); } host = host.replace(/:\d+$/, ''); host = host.replace(/^\[|\]$/g, ''); if (!host) return ''; if (/^[0-9.]+$/.test(host) || host.indexOf(':') !== -1) { return this.sanitizeName(host); } const labels = host.split('.').filter(function(part) { return part.length > 0; }); if (labels.length === 0) return ''; if (labels.length === 1) return this.sanitizeName(labels[0]); let end = labels.length - 1; const secondLevel = {com: true, co: true, net: true, org: true, gov: true, edu: true, ac: true}; if (end > 1 && secondLevel[labels[end - 1].toLowerCase()]) { end--; } return this.sanitizeName(labels[end - 1]); }, sanitizeName: function(value) { return String(value || '').trim().replace(/[^A-Za-z0-9_-]+/g, '-').replace(/^-+|-+$/g, ''); }, uniqueProviderName: function(base, editName) { const clean = this.sanitizeName(base); if (!clean) return ''; const taken = function(name) { return Object.prototype.hasOwnProperty.call(this.modelProviders, name) && name !== editName; }.bind(this); if (!taken(clean)) return clean; for (let i = 2; i < 1000; i++) { const candidate = clean + '-' + i; if (!taken(candidate)) return candidate; } return clean; }, autoFillProviderName: function() { if (this.nameDirty) return; const nameInput = document.getElementById('providerName'); if (!nameInput) return; const type = document.getElementById('providerType'); const baseUrl = document.getElementById('providerBaseUrl'); const base = this.providerBaseName(type ? type.value : '', baseUrl ? baseUrl.value : ''); nameInput.value = base ? this.uniqueProviderName(base, '') : ''; }, onProviderTypeChange: function() { this.syncProviderBaseUrlVisibility(); this.autoFillProviderName(); }, onProviderBaseUrlInput: function() { this.autoFillProviderName(); }, onProviderApiKeyInput: function() { this.autoFillProviderName(); }, onProviderNameInput: function() { this.nameDirty = true; }, saveProviderDialog: function(editName) { const type = document.getElementById('providerType').value.trim(); const apiKeyRaw = document.getElementById('providerApiKey').value.trim(); const baseUrl = document.getElementById('providerBaseUrl').value.trim(); const nameRaw = document.getElementById('providerName').value; if (!type) { alert(t('provider.required')); return; } let name = this.sanitizeName(nameRaw); if (!name) { name = this.uniqueProviderName(this.providerBaseName(type, baseUrl), editName || ''); } if (!name) { alert(t('provider.name_required')); return; } if (name !== editName && Object.prototype.hasOwnProperty.call(this.modelProviders, name)) { alert(t('provider.exists',{name:name})); return; } const provider = { type: type }; if (apiKeyRaw.charAt(0) === '$') { const env = apiKeyRaw.slice(1).trim(); if (!env) { alert(t('provider.env_required')); return; } provider.token_env = env; provider.api_key = ''; } else if (apiKeyRaw) { provider.api_key = apiKeyRaw; } else if (editName && this.modelProviders[editName]) { provider.api_key = this.modelProviders[editName].api_key || ''; } if (baseUrl && providerBaseUrlAllowed(type)) { provider.base_url = baseUrl; } const renamedFrom = editName && name !== editName ? editName : ''; if (renamedFrom) { delete this.modelProviders[renamedFrom]; } this.modelProviders[name] = provider; const selectInto = this.pendingModelSelect && !editName ? name : ''; this.pendingModelSelect = false; this.saveModelProviders(addProviderRename(this.modelRefPatch(renamedFrom,name),'model_providers',renamedFrom,name), selectInto); document.querySelector('.modal-overlay').remove(); }, modelRefPatch: function(oldName, newName) { const patch = {}; const config = appState.config || {}; ['model'].forEach(function(section) { const current = config[section]; if (!current || current.provider !== oldName) return; const copy = Object.assign({}, current); copy.provider = newName; patch[section] = copy; }); const select = document.getElementById('model_provider'); if (select && select.value === oldName) { ensureSelectOption(select, newName); select.value = newName; rememberModelProvider(); } return Object.keys(patch).length ? patch : null; }, saveModelProviders: async function(extraConfig, selectIntoModel) { try { const config = Object.assign({ model_providers: this.modelProviders }, extraConfig || {}); const payload = await request('/api/config', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ config: config, apply_wifi: false }) }); if (payload.config && payload.config.model_providers) { this.modelProviders = payload.config.model_providers; } if (payload.config) { appState.config = payload.config; } else if (appState.config) { appState.config.model_providers = this.modelProviders; } injectNamedProviderOptions(); this.renderModelProvidersList(); setBanner(t('provider.saved'), false); // Refresh model selector since provider list changed
    if (selectIntoModel) { selectModelProvider(selectIntoModel); } else { refreshModelSelectorForCurrentProvider(); } } catch (err) { setBanner(t('provider.save_failed'), true); setDetails(err.message); } }, setupEventListeners: function() {}, escapeHtml: function(str) { if (!str) return ''; return String(str) .replace(/&/g, '&amp;') .replace(/</g, '&lt;') .replace(/>/g, '&gt;') .replace(/"/g, '&quot;') .replace(/'/g, '&#039;'); }, escapeJs: function(str) { if (!str) return ''; return String(str) .replace(/\\/g, '\\\\') .replace(/'/g, "\\'") .replace(/"/g, '\\"') .replace(/\n/g, '\\n') .replace(/\r/g, '\\r'); } }; const ModelSelector = { availableModels: [], currentProvider: null, selectedModel: null, init: function() { this.setupEventListeners(); }, onProviderChange: async function(providerRef) { this.currentProvider = providerRef; if (!providerRef) { this.clearModelSelector(); return; } const providerType = this.resolveProviderType(providerRef); if (!providerType) { this.clearModelSelector(); return; } await this.loadModels(providerType); }, resolveProviderType: function(providerRef) { if (ModelProvidersManager.modelProviders[providerRef]) { return ModelProvidersManager.modelProviders[providerRef].type; } return providerRef; }, loadModels: async function(providerType) { const container = document.getElementById('modelSelectorContainer'); if (!container) return; container.innerHTML = '<div class="model-selector-loading">'+this.escapeHtml(t('provider.loading_models'))+'</div>'; try { const locale = getActiveLocale() || 'zh-CN'; const response = await fetch(`/api/models?provider=${encodeURIComponent(providerType)}&locale=${encodeURIComponent(locale)}`); if (response.status === 503) { this.availableModels = []; this.renderModelSelector(true); return; } if (!response.ok) { throw new Error(`HTTP ${response.status}`); } const data = await response.json(); this.availableModels = data.models || []; this.renderModelSelector(); } catch (err) { container.innerHTML = `<div class="model-selector-error">${t('provider.load_models_failed')}: ${this.escapeHtml(err.message)}</div>`; } }, renderModelSelector: function(agentOffline) { const container = document.getElementById('modelSelectorContainer'); if (!container) return; if (this.availableModels.length === 0 && !agentOffline) { container.innerHTML = '<div class="model-selector-empty">'+this.escapeHtml(t('provider.no_models'))+'</div>'; return; } const currentModel = document.getElementById('model_model')?.value || ''; let html = '<div class="model-selector">'; if (agentOffline) { html += `<div class="model-selector-empty">${t('provider.offline_models')}</div>`; } this.availableModels.forEach((model, index) => { const isSelected = model.id === currentModel; const isRecommended = model.recommended; html += ` <label class="model-option${isRecommended ? ' recommended' : ''}${isSelected ? ' selected' : ''}"> <input type="radio" name="model_select" value="${this.escapeHtml(model.id)}" ${isSelected ? 'checked' : ''} data-action="select-model" data-model-id="${this.escapeHtml(model.id)}"> <div class="model-info"> <div class="model-name"> ${this.escapeHtml(model.id)} ${isRecommended ? ' <span class="model-badge">⭐ '+this.escapeHtml(t('provider.recommended'))+'</span>' : ''} </div> <div class="model-desc">${this.escapeHtml(model.description || '')}</div> </div> </label> `; }); const isCustom = currentModel && !this.availableModels.find(m => m.id === currentModel); html += ` <label class="model-option custom${isCustom ? ' selected' : ''}"> <input type="radio" name="model_select" value="__custom__" ${isCustom ? 'checked' : ''} data-action="select-custom-model"> <div class="model-info"> <div class="model-name">${this.escapeHtml(t('provider.custom_model'))}</div> <input type="text" id="customModelInput" value="${isCustom ? this.escapeHtml(currentModel) : ''}" placeholder="${this.escapeHtml(t('provider.model_placeholder'))}" data-action="update-custom-model"> </div> </label> `; html += '</div>'; container.innerHTML = html; }, selectModel: function(modelId) { this.selectedModel = modelId; const modelInput = document.getElementById('model_model'); if (modelInput) { modelInput.value = modelId; } }, selectCustom: function() { const customInput = document.getElementById('customModelInput'); if (customInput) { customInput.focus(); this.updateCustomModel(customInput.value); } }, updateCustomModel: function(value) { this.selectedModel = value; const modelInput = document.getElementById('model_model'); if (modelInput) { modelInput.value = value; } }, clearModelSelector: function() { const container = document.getElementById('modelSelectorContainer'); if (container) { container.innerHTML = ''; } }, setupEventListeners: function() { // Delegated listener survives syncConditionalSelectField element replacement
    document.addEventListener('change', (e) => { if (e.target.id !== 'model_provider') return; rememberModelProvider(); updateProviderActionState('model'); this.onProviderChange(e.target.value); }); }, escapeHtml: function(str) { if (!str) return ''; return String(str) .replace(/&/g, '&amp;') .replace(/</g, '&lt;') .replace(/>/g, '&gt;') .replace(/"/g, '&quot;') .replace(/'/g, '&#039;'); }, escapeJs: function(str) { if (!str) return ''; return String(str) .replace(/\\/g, '\\\\') .replace(/'/g, "\\'") .replace(/"/g, '\\"') .replace(/\n/g, '\\n') .replace(/\r/g, '\\r'); } }; // Initialize on DOM ready
    function escRecordHtml(value){return String(value==null?'':value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');}
    function escRecordJs(value){return String(value==null?'':value).replace(/\\/g,'\\\\').replace(/'/g,"\\'");}
    function hostLabel(url){let host=String(url||'').trim();const scheme=host.indexOf(':'+'/'+'/');if(scheme!==-1){host=host.slice(scheme+3);}host=host.split('/')[0].split('?')[0];const at=host.lastIndexOf('@');if(at!==-1){host=host.slice(at+1);}host=host.replace(/:\d+$/,'');host=host.replace(/^\[|\]$/g,'');if(!host)return '';if(/^[0-9.]+$/.test(host)||host.indexOf(':')!==-1){return sanitizeRecordName(host);}const labels=host.split('.').filter(function(part){return part.length>0;});if(labels.length===0)return '';if(labels.length===1)return sanitizeRecordName(labels[0]);let end=labels.length-1;const secondLevel={com:true,co:true,net:true,org:true,gov:true,edu:true,ac:true};if(end>1&&secondLevel[labels[end-1].toLowerCase()]){end--;}return sanitizeRecordName(labels[end-1]);}
    function sanitizeRecordName(value){return String(value==null?'':value).trim().replace(/[^A-Za-z0-9_-]+/g,'-').replace(/^-+|-+$/g,'');}
    function ensureBlankTypeOption(section){const fields=selectFieldOptions[section];if(!fields||!Array.isArray(fields.type))return;if(fields.type.length&&optionValue(fields.type[0])==='')return;fields.type=[{value:'',label:t('provider.select'),providers:[]}].concat(fields.type);}
    function providerRecordFieldsHtml(section,record){const fields=recordSectionFields[section]||[];const opts=selectFieldOptions[section]||{};let html='';fields.forEach(function(item){const key=item[0];const secret=item[2];const id=section+'_'+key;const raw=record&&record[key]!=null?String(record[key]):'';const shown=key==='api_key'&&record&&record.token_env?'$'+record.token_env:raw;html+='<div class="field"><label>'+escRecordHtml(key)+'</label>';if(Array.isArray(opts[key])&&opts[key].length){html+='<select id="'+id+'"></select>';}else if(secret){html+='<input type="password" id="'+id+'" value="'+escRecordHtml(shown)+'" autocomplete="off">';}else{html+='<input type="text" id="'+id+'" value="'+escRecordHtml(shown)+'">';}if(key==='api_key'){html+='<small>'+escRecordHtml(t('provider.api_key_help'))+'</small>';}html+='</div>';});return html;}
    function createProviderRecordsManager(spec){return {
      spec:spec, records:{}, nameDirty:false, pendingSelect:false,
      init:function(){},
      load:function(records){this.records=records||{};},
      render:function(){const container=byId(this.spec.listId);if(!container)return;const names=Object.keys(this.records).sort();if(names.length===0){container.innerHTML='<div class="providers-empty"><p>'+escRecordHtml(t('provider.none'))+'</p><p>'+escRecordHtml(t('provider.none_help'))+'</p></div>';return;}const self=this;container.innerHTML=names.map(function(name){return self.cardHtml(name,self.records[name]||{});}).join('');},
      cardHtml:function(name,record){const g=this.spec.global;let rows='<div class="provider-info-row"><span class="provider-info-label">'+escRecordHtml(t('provider.type_short'))+':</span><span>'+escRecordHtml(record.type||'')+'</span></div>';if(record.token_env||record.api_key){rows+='<div class="provider-info-row"><span class="provider-info-label">'+escRecordHtml(t('provider.api_key'))+':</span><span>'+escRecordHtml(record.token_env?'$'+record.token_env:record.api_key)+'</span></div>';}const detail=this.spec.detailKeys||[];const self=this;detail.forEach(function(key){if(!record[key])return;rows+='<div class="provider-info-row"><span class="provider-info-label">'+escRecordHtml(key)+':</span><span>'+escRecordHtml(record[key])+'</span></div>';});return '<div class="provider-card"><div class="provider-card-head"><div class="provider-name">'+escRecordHtml(name)+'</div><div class="provider-actions"><button class="button ghost small" data-action="voice-provider-edit" data-provider-manager="'+g+'" data-provider-name="'+escRecordHtml(name)+'">'+escRecordHtml(t('action.edit'))+'</button><button class="button ghost small error" data-action="voice-provider-delete" data-provider-manager="'+g+'" data-provider-name="'+escRecordHtml(name)+'">'+escRecordHtml(t('action.delete'))+'</button></div></div><div class="provider-info">'+rows+'</div></div>';},
      addRecord:function(options){this.pendingSelect=!!(options&&options.selectIntoRef);this.showDialog(null);},
      editRecord:function(name){const record=this.records[name];if(!record)return;this.showDialog(name,record);},
      deleteRecord:function(name){if(!window.confirm(t('provider.delete_confirm',{name:name})))return;delete this.records[name];this.save();},
      showDialog:function(editName,editRecord){const section=this.spec.section;const record=editRecord||{};const isEdit=!!editName;this.nameDirty=isEdit;const g=this.spec.global;const dialog=document.createElement('div');dialog.className='modal-overlay';dialog.innerHTML='<div class="modal-dialog"><div class="modal-header"><h3>'+escRecordHtml(isEdit?t('provider.edit'):t('provider.add'))+'</h3><button class="modal-close" data-action="close-modal">&times;</button></div><div class="modal-body">'+providerRecordFieldsHtml(section,record)+'<div class="field"><label>'+escRecordHtml(t('provider.name'))+'</label><input type="text" id="'+section+'_record_name" value="'+escRecordHtml(editName||'')+'" placeholder="'+escRecordHtml(t('provider.auto'))+'" data-action="voice-provider-name" data-provider-manager="'+g+'"><small>'+escRecordHtml(t('provider.name_help'))+'</small></div></div><div class="modal-footer"><button class="button ghost" data-action="close-modal">'+escRecordHtml(t('action.cancel'))+'</button><button class="button primary" data-action="voice-provider-save" data-provider-manager="'+g+'" data-edit-name="'+escRecordHtml(editName||'')+'">'+escRecordHtml(isEdit?t('provider.save_changes'):t('provider.add'))+'</button></div></div>';document.body.appendChild(dialog);this.hydrateDialog(record);},
      hydrateDialog:function(record){const section=this.spec.section;const fields=recordSectionFields[section]||[];const opts=selectFieldOptions[section]||{};ensureBlankTypeOption(section);hydrateSelectField(section,'type',record.type==null?'':String(record.type),true);const typeEl=byId(section+'_type');if(typeEl){const self=this;typeEl.addEventListener('change',function(){self.onTypeChange();});}fields.forEach(function(item){const key=item[0];if(key==='type')return;if(!Array.isArray(opts[key])||!opts[key].length)return;hydrateSelectField(section,key,record[key]==null?'':String(record[key]),true);});applyFieldVisibility(true);this.autoFillName();},
      onTypeChange:function(){const section=this.spec.section;const opts=selectFieldOptions[section]||{};(recordSectionFields[section]||[]).forEach(function(item){const key=item[0];if(key==='type')return;if(!Array.isArray(opts[key])||!opts[key].length)return;const el=byId(section+'_'+key);hydrateSelectField(section,key,el?el.value:'',true);});applyFieldVisibility(true);this.autoFillName();},
      onNameInput:function(){this.nameDirty=true;},
      sanitizeName:function(value){return String(value==null?'':value).trim().replace(/[^A-Za-z0-9_-]+/g,'-').replace(/^-+|-+$/g,'');},
      uniqueName:function(base,editName){const clean=this.sanitizeName(base);if(!clean)return '';const self=this;const taken=function(name){return Object.prototype.hasOwnProperty.call(self.records,name)&&name!==editName;};if(!taken(clean))return clean;for(let i=2;i<1000;i++){const candidate=clean+'-'+i;if(!taken(candidate))return candidate;}return clean;},
      autoFillName:function(){if(this.nameDirty)return;const section=this.spec.section;const nameEl=byId(section+'_record_name');if(!nameEl)return;const typeEl=byId(section+'_type');const base=typeEl?String(typeEl.value||''):'';nameEl.value=base?this.uniqueName(base,''):'';},
      readDialog:function(){const section=this.spec.section;const record={};(recordSectionFields[section]||[]).forEach(function(item){const key=item[0];const el=byId(section+'_'+key);if(!el)return;const field=el.closest?el.closest('.field'):null;if(field&&field.classList.contains('hidden'))return;const value=String(el.value||'').trim();if(value!=='')record[key]=value;});return record;},
      saveDialog:function(editName){const section=this.spec.section;const record=this.readDialog();if(!record.type){alert(t('provider.type_required'));return;}const nameEl=byId(section+'_record_name');let name=this.sanitizeName(nameEl?nameEl.value:'');if(!name){name=this.uniqueName(record.type,editName||'');}if(!name){alert(t('provider.name_required'));return;}if(name!==editName&&Object.prototype.hasOwnProperty.call(this.records,name)){alert(t('provider.exists',{name:name}));return;}
        const apiKeyRaw=record.api_key==null?'':String(record.api_key);if(apiKeyRaw.charAt(0)==='$'){const env=apiKeyRaw.slice(1).trim();if(!env){alert(t('provider.env_required'));return;}record.token_env=env;delete record.api_key;}else if(!apiKeyRaw&&editName&&this.records[editName]){const previous=this.records[editName];if(previous.api_key)record.api_key=previous.api_key;if(previous.token_env)record.token_env=previous.token_env;}
        const renamedFrom=editName&&name!==editName?editName:'';if(renamedFrom){delete this.records[renamedFrom];}this.records[name]=record;const selectInto=this.pendingSelect&&!editName?name:'';this.pendingSelect=false;this.save(addProviderRename(renamedFrom?this.refPatch(renamedFrom,name):null,section,renamedFrom,name),selectInto);const overlay=document.querySelector('.modal-overlay');if(overlay)overlay.remove();},
      refPatch:function(oldName,newName){const config=appState.config||{};const current=config[this.spec.refSection];if(!current||current.provider!==oldName)return null;const copy=Object.assign({},current);copy.provider=newName;const el=byId(this.spec.refFieldId);if(el&&el.value===oldName){ensureSelectOption(el,newName);el.value=newName;}const patch={};patch[this.spec.refSection]=copy;return patch;},
      save:async function(extraConfig,selectIntoRef){try{const body={};body[this.spec.configKey]=this.records;const config=Object.assign(body,extraConfig||{});const payload=await request('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({config:config,apply_wifi:false})});if(payload.config&&payload.config[this.spec.configKey]){this.records=payload.config[this.spec.configKey];}if(payload.config){appState.config=payload.config;}else if(appState.config){appState.config[this.spec.configKey]=this.records;}injectVoiceProviderOptions(this.spec);setBanner(t('provider.saved'),false);if(selectIntoRef){const el=byId(this.spec.refFieldId);if(el){ensureSelectOption(el,selectIntoRef);el.value=selectIntoRef;}}applyFieldVisibility(true);updateProviderActionState(this.spec.refSection);}catch(err){setBanner(t('provider.save_failed'),true);setDetails(err.message);}}
    };}
    const TTS_PROVIDER_SPEC={global:'TtsProvidersManager',section:'tts_providers',configKey:'tts_providers',refSection:'tts',refFieldId:'tts_provider',addBtnId:'addTtsProviderBtn'};
    const STT_PROVIDER_SPEC={global:'SttProvidersManager',section:'stt_providers',configKey:'stt_providers',refSection:'stt',refFieldId:'stt_provider',addBtnId:'addSttProviderBtn'};
    const TtsProvidersManager=createProviderRecordsManager(TTS_PROVIDER_SPEC);
    const SttProvidersManager=createProviderRecordsManager(STT_PROVIDER_SPEC);
    function voiceRecordOptions(records){return Object.keys(records||{}).sort().map(function(name){const type=String((records[name]||{}).type||'');return {value:name,label:type?name+' ('+type+')':name,providers:[]};});}
    function injectVoiceProviderOptions(spec){const manager=spec.global==='TtsProvidersManager'?TtsProvidersManager:SttProvidersManager;const fields=selectFieldOptions[spec.refSection];if(!fields||!Array.isArray(fields.provider))return;const el=byId(spec.refFieldId);const current=el?String(el.value||''):'';const options=voiceRecordOptions(manager.records);const known={};options.forEach(function(option){known[optionValue(option)]=true;});if(current&&!known[current]){options.push({value:current,label:current,providers:[]});}options.unshift({value:'',label:'-- Select Provider --',providers:[]});fields.provider=options;hydrateSelectField(spec.refSection,'provider',current,true);rememberVoiceProvider(spec);updateProviderActionState(spec.refSection);}
    const lastVoiceProviderValue={};
    function rememberVoiceProvider(spec){const el=byId(spec.refFieldId);if(el){lastVoiceProviderValue[spec.refSection]=el.value;}}
    function restoreVoiceProviderValue(spec){const el=byId(spec.refFieldId);if(!el)return;const remembered=lastVoiceProviderValue[spec.refSection]||'';ensureSelectOption(el,remembered);el.value=remembered;}
    function syncVoiceProvidersFromConfig(){const config=appState.config||{};TtsProvidersManager.load(config.tts_providers||{});SttProvidersManager.load(config.stt_providers||{});injectVoiceProviderOptions(TTS_PROVIDER_SPEC);injectVoiceProviderOptions(STT_PROVIDER_SPEC);}
    function bindVoiceProviderSelect(spec){document.addEventListener('change',function(e){if(!e.target||e.target.id!==spec.refFieldId)return;rememberVoiceProvider(spec);updateProviderActionState(spec.refSection);});}
    function providerContext(kind){if(kind==='model'){return {manager:ModelProvidersManager,records:ModelProvidersManager.modelProviders,configKey:'model_providers',refSection:'model',refFieldId:'model_provider',editBtnId:'editProviderBtn',deleteBtnId:'deleteProviderBtn',add:function(){ModelProvidersManager.addProvider();},edit:function(name){ModelProvidersManager.editProvider(name);},refPatch:function(oldName,newName){return ModelProvidersManager.modelRefPatch(oldName,newName);}};}const spec=kind==='tts'?TTS_PROVIDER_SPEC:STT_PROVIDER_SPEC;const manager=kind==='tts'?TtsProvidersManager:SttProvidersManager;return {manager:manager,records:manager.records,configKey:spec.configKey,refSection:spec.refSection,refFieldId:spec.refFieldId,editBtnId:kind==='tts'?'editTtsProviderBtn':'editSttProviderBtn',deleteBtnId:kind==='tts'?'deleteTtsProviderBtn':'deleteSttProviderBtn',add:function(){manager.addRecord();},edit:function(name){manager.editRecord(name);},refPatch:function(oldName,newName){return manager.refPatch(oldName,newName);}};}
    function selectedProviderName(kind){const context=providerContext(kind);const el=byId(context.refFieldId);const name=el?String(el.value||''):'';return Object.prototype.hasOwnProperty.call(context.records||{},name)?name:'';}
    function updateProviderActionState(kind){const context=providerContext(kind);const enabled=!!selectedProviderName(kind);const edit=byId(context.editBtnId);const remove=byId(context.deleteBtnId);if(edit)edit.disabled=!enabled;if(remove)remove.disabled=!enabled;}
    function updateAllProviderActionStates(){updateProviderActionState('model');updateProviderActionState('tts');updateProviderActionState('stt');}
    function editSelectedProvider(kind){const context=providerContext(kind);const name=selectedProviderName(kind);if(name)context.edit(name);}
    async function deleteSelectedProvider(kind){const context=providerContext(kind);const name=selectedProviderName(kind);if(!name)return;const records=context.records||{};const remainingNames=Object.keys(records).filter(function(candidate){return candidate!==name;}).sort();const storedSection=(appState.config&&appState.config[context.refSection])||{};const selected=byId(context.refFieldId);const originalSelectedValue=selected?selected.value:'';const selectedInForm=!!(selected&&selected.value===name);const storedInUse=storedSection.provider===name;if(storedInUse&&remainingNames.length===0){alert(t('provider.in_use'));return;}const question=storedInUse?t('provider.delete_in_use_confirm'):t('provider.delete_confirm',{name:name});if(!window.confirm(question))return;const replacement=storedInUse?remainingNames[0]:'';const uiReplacement=selectedInForm?(replacement||storedSection.provider||remainingNames[0]||''):'';const nextRecords=Object.assign({},records);delete nextRecords[name];const patch=replacement?context.refPatch(name,replacement):null;const body=Object.assign({},patch||{});body[context.configKey]=nextRecords;try{const payload=await request('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({config:body,apply_wifi:false})});if(payload.config){appState.config=payload.config;}else if(appState.config){appState.config[context.configKey]=nextRecords;}if(selectedInForm&&selected){ensureSelectOption(selected,uiReplacement);selected.value=uiReplacement;}syncModelProvidersFromConfig();syncVoiceProvidersFromConfig();syncModelSelectorSummary();updateAllProviderActionStates();setBanner(t('provider.saved'),false);}catch(err){if(selected){ensureSelectOption(selected,originalSelectedValue);selected.value=originalSelectedValue;}rememberModelProvider();updateAllProviderActionStates();setBanner(t('provider.save_failed'),true);setDetails(err.message);}}
    function syncModelSelectorSummary(){const input=byId('model_model');const summary=byId('modelSelectorSummary');if(!summary)return;const value=input?String(input.value||'').trim():'';summary.textContent=value||t('provider.choose_model');summary.title=value;}

export function initProviders(){bindVoiceProviderSelect(TTS_PROVIDER_SPEC);bindVoiceProviderSelect(STT_PROVIDER_SPEC);document.addEventListener('change',function(event){const target=event.target;if(!target)return;if(target.name==='model_select'){if(target.dataset.action==='select-model')ModelSelector.selectModel(target.dataset.modelId||target.value);else if(target.dataset.action==='select-custom-model')ModelSelector.selectCustom();syncModelSelectorSummary();if(target.value!=='__custom__'){const details=byId('modelSelectorDetails');if(details)details.open=false;}}else if(target.id==='customModelInput'){ModelSelector.updateCustomModel(target.value);syncModelSelectorSummary();}});document.addEventListener('input',function(event){const target=event.target;if(!target)return;if(target.id==='customModelInput'){ModelSelector.updateCustomModel(target.value);syncModelSelectorSummary();}else if(target.dataset.action==='model-provider-api-key'){ModelProvidersManager.onProviderApiKeyInput();}else if(target.dataset.action==='model-provider-base-url'){ModelProvidersManager.onProviderBaseUrlInput();}else if(target.dataset.action==='model-provider-name'){ModelProvidersManager.onProviderNameInput();}else if(target.dataset.action==='voice-provider-name'){const manager=target.dataset.providerManager==='TtsProvidersManager'?TtsProvidersManager:SttProvidersManager;manager.onNameInput();}});document.addEventListener('change',function(event){const target=event.target;if(target&&target.dataset.action==='model-provider-type')ModelProvidersManager.onProviderTypeChange();});ModelProvidersManager.init();ModelSelector.init();TtsProvidersManager.init();SttProvidersManager.init();updateAllProviderActionStates();syncModelSelectorSummary();}
export function handleProviderAction(action,target){const managerName=target.dataset.providerManager;const voiceManager=managerName==='TtsProvidersManager'?TtsProvidersManager:SttProvidersManager;const name=target.dataset.providerName||'';if(action==='model-provider-edit')ModelProvidersManager.editProvider(name);else if(action==='model-provider-delete')ModelProvidersManager.deleteProvider(name);else if(action==='model-provider-save')ModelProvidersManager.saveProviderDialog(target.dataset.editName||'');else if(action==='voice-provider-edit')voiceManager.editRecord(name);else if(action==='voice-provider-delete')voiceManager.deleteRecord(name);else if(action==='voice-provider-save')voiceManager.saveDialog(target.dataset.editName||'');else if(action==='close-modal'){const overlay=target.closest('.modal-overlay');if(overlay)overlay.remove();}}
registerRuntime({initProviders,handleProviderAction});
export { syncModelProvidersFromConfig, ModelProvidersManager, ModelSelector, TtsProvidersManager, SttProvidersManager, syncVoiceProvidersFromConfig, rememberModelProvider, restoreModelProviderValue, updateProviderActionState, updateAllProviderActionStates, editSelectedProvider, deleteSelectedProvider, syncModelSelectorSummary };
registerRuntime({ syncModelProvidersFromConfig, ModelProvidersManager, ModelSelector, TtsProvidersManager, SttProvidersManager, syncVoiceProvidersFromConfig, rememberModelProvider, restoreModelProviderValue, updateProviderActionState, updateAllProviderActionStates, editSelectedProvider, deleteSelectedProvider, syncModelSelectorSummary });
