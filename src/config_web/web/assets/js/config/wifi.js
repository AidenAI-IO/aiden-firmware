import {appState, byId, registerRuntime, runtimeFunction} from './state.js';
const request=runtimeFunction('request');const setBanner=runtimeFunction('setBanner');const setDetails=runtimeFunction('setDetails');const localizedText=runtimeFunction('localizedText');const t=runtimeFunction('t');
function savedWifiNetworks(){const wifi=appState.wifi||{};if(Array.isArray(wifi.networks))return wifi.networks;if(wifi.ssid)return [{ssid:wifi.ssid,psk:wifi.psk||'',priority:wifi.priority||1}];return [];}
    function savedWifiBySsid(ssid){const nets=savedWifiNetworks();for(let i=0;i<nets.length;i++){if(nets[i].ssid===ssid)return nets[i];}return null;}
    function connectedWifiSsid(){const status=appState.wifiStatus||{};return status.connected&&status.ssid?status.ssid:'';}
    function addWifiName(names,seen,name){if(name&&!seen[name]){seen[name]=true;names.push(name);}}
    function displayWifiNames(){const names=[];const seen={};savedWifiNetworks().slice().sort(function(a,b){return Number(b.priority||0)-Number(a.priority||0);}).forEach(function(n){addWifiName(names,seen,n.ssid);});addWifiName(names,seen,connectedWifiSsid());(appState.networks||[]).forEach(function(name){addWifiName(names,seen,name);});return names;}
    function visibleWifiNames(names){const connected=connectedWifiSsid();if(!connected||appState.wifiListExpanded)return names;return names.filter(function(name){return name===connected;});}
    function toggleWifiListExpanded(){appState.wifiListExpanded=!appState.wifiListExpanded;renderWifiList();}
    function appendWifiToggleRow(list,totalCount,visibleCount){const hiddenCount=Math.max(0,totalCount-visibleCount);if(!connectedWifiSsid()||totalCount<=1)return;const row=document.createElement('div');row.className='wifi-toggle-row';const btn=document.createElement('button');btn.type='button';btn.className='button ghost';btn.textContent=appState.wifiListExpanded?'Hide other Wi-Fi':'Show other Wi-Fi ('+String(hiddenCount)+')';btn.dataset.action='toggle-wifi-list';row.appendChild(btn);list.appendChild(row);}
    function renderWifiList(){const list=byId('wifiList');list.innerHTML='';const names=displayWifiNames();const visibleNames=visibleWifiNames(names);if(!visibleNames.length){const empty=document.createElement('div');empty.className='wifi-row';const left=document.createElement('div');const title=document.createElement('div');title.className='wifi-main';title.textContent='No networks scanned';const meta=document.createElement('div');meta.className='wifi-meta';meta.textContent='Click "Rescan" to try again';left.appendChild(title);left.appendChild(meta);empty.appendChild(left);list.appendChild(empty);return;}const saved=savedWifiNetworks();let maxPriority=0;saved.forEach(function(n){maxPriority=Math.max(maxPriority,Number(n.priority||0));});visibleNames.forEach(function(name){const savedNet=savedWifiBySsid(name);const item=document.createElement('div');let cls='wifi-row';let meta=[];let state='';if(appState.wifiStatus&&appState.wifiStatus.connected&&appState.wifiStatus.ssid===name){cls+=' current';meta.push(appState.wifiStatus.ip_address?('Connected · '+appState.wifiStatus.ip_address):'Connected');state='✓';}if(savedNet){cls+=' saved';meta.push(Number(savedNet.priority||0)===maxPriority?'Saved · highest priority':'Saved · priority '+String(savedNet.priority||0));}item.className=cls;const left=document.createElement('button');left.type='button';left.className='wifi-connect';left.dataset.action=savedNet?'connect-saved-wifi':'open-wifi-modal';left.dataset.ssid=name;const title=document.createElement('div');title.className='wifi-main';title.textContent=name;left.appendChild(title);if(meta.length){const metaEl=document.createElement('div');metaEl.className='wifi-meta';metaEl.textContent=meta.join(' · ');left.appendChild(metaEl);}const right=document.createElement('div');right.className='wifi-state';right.textContent=state;if(savedNet){const forget=document.createElement('button');forget.type='button';forget.className='button ghost';forget.textContent='Forget';forget.dataset.action='forget-wifi';forget.dataset.ssid=name;right.appendChild(forget);}item.appendChild(left);item.appendChild(right);list.appendChild(item);});appendWifiToggleRow(list,names.length,visibleNames.length);}
    async function scanWifi(showBanner){byId('scanBtn').disabled=true;try{const payload=await request('/api/wifi/scan',{method:'POST'});appState.networks=payload.networks||[];renderWifiList();if(showBanner!==false){setBanner('Wi-Fi list refreshed.',false);setDetails('');}}catch(err){setBanner('Failed to scan Wi-Fi.',true);setDetails(err.message);}finally{byId('scanBtn').disabled=false;}}
    function openWifiModal(ssid,focusPassword){const saved=savedWifiBySsid(ssid);appState.selectedSsid=ssid;byId('wifiModalText').textContent='Connect to "'+ssid+'"';byId('wifiPasswordInput').value=saved&&saved.psk?saved.psk:'';byId('wifiModal').classList.add('show');if(focusPassword!==false){byId('wifiPasswordInput').focus();}}
    function closeWifiModal(){byId('wifiModal').classList.remove('show');appState.selectedSsid='';}
    function connectSavedWifi(ssid){const saved=savedWifiBySsid(ssid);openWifiModal(ssid,false);if(!saved)return;byId('wifiModalText').textContent='Connecting to "'+ssid+'"...';const btn=byId('connectWifiBtn');if(btn){btn.disabled=true;btn.textContent='Connecting';}setTimeout(function(){connectWifi(ssid,saved.psk||'');},0);}
    async function connectWifi(ssid,psk){const btn=byId('connectWifiBtn');if(btn){btn.disabled=true;btn.textContent='Connecting';}byId('wifiModalText').textContent='Connecting to "'+ssid+'"...';try{const payload=await request('/api/wifi/connect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ssid:ssid,psk:psk})});appState.wifi=payload.wifi||{ssid:ssid,psk:psk};appState.wifiStatus=payload.wifi_status||{};const connected=!!payload.ok&&connectedWifiSsid()===ssid;if(connected){appState.wifiListExpanded=false;}renderWifiList();if(connected){closeWifiModal();setBanner(t('wifi.connected_to',{ssid:ssid}),false);setDetails('');}else{byId('wifiModalText').textContent='Failed to connect to "'+ssid+'", please check password and try again.';setBanner('Failed to connect to Wi-Fi.',true);setDetails(payload.wifi_apply&&payload.wifi_apply.output?payload.wifi_apply.output:'Connection not confirmed to target network.');}}catch(err){byId('wifiModalText').textContent='Failed to connect to "'+ssid+'", please check password and try again.';setBanner('Failed to connect to Wi-Fi.',true);setDetails(err.message);}finally{if(btn){btn.disabled=false;btn.textContent='Connect';}}}
    function connectSelectedWifi(){return connectWifi(appState.selectedSsid,byId('wifiPasswordInput').value);}
    async function forgetWifi(ssid){if(!window.confirm(localizedText('Forget "'+ssid+'"?')))return;try{const payload=await request('/api/wifi/forget',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ssid:ssid})});appState.wifi=payload.wifi||{};appState.wifiStatus=payload.wifi_status||{};renderWifiList();if(payload.ok!==false){setBanner('Forgot "'+ssid+'".',false);setDetails('');}else{setBanner('Forgot "'+ssid+'", but runtime apply failed.',true);setDetails(payload.wifi_apply&&payload.wifi_apply.output?payload.wifi_apply.output:'Network removed from saved list, but runtime config update failed.');}}catch(err){setBanner('Failed to forget Wi-Fi.',true);setDetails(err.message);}}

export { connectedWifiSsid, renderWifiList, toggleWifiListExpanded, scanWifi, openWifiModal, closeWifiModal, connectSavedWifi, connectSelectedWifi, forgetWifi };
registerRuntime({ connectedWifiSsid, renderWifiList, toggleWifiListExpanded, scanWifi, openWifiModal, closeWifiModal, connectSavedWifi, connectSelectedWifi, forgetWifi });
