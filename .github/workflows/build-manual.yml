name: Build (manual / hosted)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      runner: ubuntu-latest
      free_disk_space: true
    secrets: inherit
