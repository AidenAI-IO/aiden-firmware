#!/bin/sh
set -eu

printf 'login: '
IFS= read -r login_user

case "${login_user}" in
    ''|-*|*[!A-Za-z0-9_.-]*)
        printf 'Invalid login name\r\n' >&2
        exit 1
        ;;
esac

exec /bin/su --login -- "${login_user}"
