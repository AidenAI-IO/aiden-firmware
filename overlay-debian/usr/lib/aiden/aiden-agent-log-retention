#!/bin/sh
set -eu

python_bin=${AIDEN_PYTHON_BIN:-/usr/bin/python3}
retention_program=/usr/lib/aiden/libexec/agent_log_retention.py
if [ ! -r "${retention_program}" ]; then
    retention_program="$(dirname "$0")/libexec/agent_log_retention.py"
fi

exec "${python_bin}" "${retention_program}"
